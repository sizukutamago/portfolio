language: php

php:
 - 7.1.10

before_script:
 - composer self-update
 - composer install --no-interaction
 - cp .env.travis .env
 - php artisan key:generate
 - npm install

script:
 - ./vendor/bin/phpunit

services:
 - mysql

cache:
 directories:
  - node_modules
  - vendor
